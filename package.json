{
  "name": "iLigtas",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && node node_modules/vite/bin/vite.js build && rm -rf ../dist && cp -r dist ../dist",
    "dev": "cd client && npm install && npm run dev",
    "lint": "cd client && npm run lint"
  }
}
